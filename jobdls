job('example') {
  steps {
    batchFile('echo Hello World!')
    batchFile('echo Sucess!')
    batchFile('echo Sucesssss!')
    batchFile('echo git commit changes!')
    batchFile('echo git commit changes shown!')
    
  }
}
