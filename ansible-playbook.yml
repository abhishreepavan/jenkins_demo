---
 - hosts: nodes
   tasks:
       
     - name: example copying file with owner and permissions
       copy:
           src: /etc/nginx/nginx.conf
           dest: /home/ec2-user
           owner: foo
           group: foo
           mode: 0644
