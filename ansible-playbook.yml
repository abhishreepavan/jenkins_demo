---
 - hosts: 43.204.212.230
   tasks:
   
     - name: Copy each file over that matches the given pattern
       copy:
        src = "{{src}}"
        dest= "{{dest}}"
           
     - name: Restart service
       ansible.builtin.service:
          name: "{{service}}"
          state: restarted
